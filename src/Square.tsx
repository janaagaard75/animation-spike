import React from "react"
import { View } from "react-native"

export const Square = () => (
  <View
    style={{
      backgroundColor: "red",
      borderRadius: 5,
      height: 100,
      width: 100
    }}
  ></View>
)
